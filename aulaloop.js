//automizar tarefas repetitivas
//FOR LOOP = CONTADOR
//for loop keeps running while condition is TRUE
//tem 3 partes (valor inicial de um contador,  a segunda parte é uma condição lógica que é avaliada antes de cada iteração de loop,
//se essa condição lógica é verdadeira, então o loop continua, se for falsa, o loop termina.
//a terceira parte atualiza o contador a cada iteração do loop)

for (let rep = 1; rep <= 10; rep = rep + 1) {
  console.log(`lifting weights repetition ${rep} 🏃‍♂️`);
}

//rep++ é a mesma coisa que rep = rep + 1
